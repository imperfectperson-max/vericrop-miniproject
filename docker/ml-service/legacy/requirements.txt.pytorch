fastapi==0.109.1
uvicorn[standard]==0.24.0

# Use a PyTorch wheel that matches the Python runtime in the image
# Example: use a 2.0.x+cpu wheel if that is available for Python 3.11
torch==2.0.1+cpu
torchvision==0.15.2+cpu

pillow==10.3.0
python-multipart==0.0.6

# PyTorch CPU wheel index - required for the +cpu wheel specifier above
-f https://download.pytorch.org/whl/cpu/torch_stable.html
