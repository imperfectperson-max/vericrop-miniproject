<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.shape.Circle?>

<!-- Common Navigation Bar for all screens -->
<HBox xmlns="http://javafx.com/javafx/17" xmlns:fx="http://javafx.com/fxml/1"
      spacing="15" alignment="CENTER_LEFT"
      style="-fx-background-color: linear-gradient(to right, #2E8B57, #3CB371); -fx-padding: 10 20;">

    <!-- Logo and Title -->
    <HBox spacing="10" alignment="CENTER">
        <Circle radius="20" fill="white"/>
        <VBox spacing="0">
            <Label text="VeriCrop" style="-fx-font-size: 20px; -fx-font-weight: bold; -fx-text-fill: white;"/>
            <Label text="Supply Chain Management" style="-fx-font-size: 10px; -fx-text-fill: #E0F2F1;"/>
        </VBox>
    </HBox>

    <Region HBox.hgrow="ALWAYS"/>

    <!-- Navigation Buttons -->
    <HBox spacing="8" alignment="CENTER">
        <Button fx:id="navProducerButton" text="🌾 Producer"
                styleClass="nav-button"
                style="-fx-background-color: rgba(255, 255, 255, 0.2); -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 6; -fx-padding: 6 12; -fx-cursor: hand;"
                onAction="#handleNavProducer"/>
        <Button fx:id="navLogisticsButton" text="🚚 Logistics"
                styleClass="nav-button"
                style="-fx-background-color: rgba(255, 255, 255, 0.2); -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 6; -fx-padding: 6 12; -fx-cursor: hand;"
                onAction="#handleNavLogistics"/>
        <Button fx:id="navConsumerButton" text="👥 Consumer"
                styleClass="nav-button"
                style="-fx-background-color: rgba(255, 255, 255, 0.2); -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 6; -fx-padding: 6 12; -fx-cursor: hand;"
                onAction="#handleNavConsumer"/>
        <Button fx:id="navAnalyticsButton" text="📊 Analytics"
                styleClass="nav-button"
                style="-fx-background-color: rgba(255, 255, 255, 0.2); -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 6; -fx-padding: 6 12; -fx-cursor: hand;"
                onAction="#handleNavAnalytics"/>
    </HBox>

    <!-- User Info and Actions -->
    <HBox spacing="8" alignment="CENTER">
        <!-- Alerts Badge -->
        <Button fx:id="navAlertsButton" text="🔔"
                style="-fx-background-color: rgba(255, 255, 255, 0.2); -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 6; -fx-padding: 6 10; -fx-cursor: hand;"
                onAction="#handleNavAlerts"/>
        <Label fx:id="alertsBadge" text="0" visible="false"
               style="-fx-background-color: #F44336; -fx-text-fill: white; -fx-padding: 2 6; -fx-background-radius: 10; -fx-font-size: 10px; -fx-font-weight: bold;"/>
        
        <!-- Messages Badge -->
        <Button fx:id="navInboxButton" text="✉️ Inbox"
                style="-fx-background-color: rgba(255, 255, 255, 0.2); -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 6; -fx-padding: 6 10; -fx-cursor: hand;"
                onAction="#handleNavInbox"/>
        <Label fx:id="messagesBadge" text="0" visible="false"
               style="-fx-background-color: #FF5722; -fx-text-fill: white; -fx-padding: 2 6; -fx-background-radius: 10; -fx-font-size: 10px; -fx-font-weight: bold;"/>

        <!-- User Name -->
        <Label fx:id="usernameLabel" text="User"
               style="-fx-text-fill: white; -fx-font-weight: bold; -fx-padding: 6 10;"/>

        <!-- Logout Button -->
        <Button fx:id="logoutButton" text="🚪 Logout"
                style="-fx-background-color: #D32F2F; -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 6; -fx-padding: 6 12; -fx-cursor: hand;"
                onAction="#handleLogout"/>
    </HBox>
</HBox>
